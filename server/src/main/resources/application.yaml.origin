spring:
  application:
    name: server
  datasource:
    driver-class-name: org.postgresql.Driver
    url:
    username:
    password:
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  security:
    jwt-secret:

springdoc:
  api-docs:
    path: /api-docs
